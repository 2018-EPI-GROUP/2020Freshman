一个矩阵元素的“鞍点”是指该位置上的元素值在该行上最大、在该列上最小。

本题要求编写程序，求一个给定的n阶方阵的鞍点。


#include<stdio.h>
int main(){
      int n,i,j;
      scanf("%d",&n);
      int a[n][n];
      //int c[n],b[n];
      for(i=0;i<n;i++){
            for(j=0;j<n;j++){
                  scanf("%d",&a[i][j]);
            }
      }
      int max,min;
      int cnt,k;
      int flag=0;
      for(i=0;i<n;i++){
            max=0;
            min=100;
            cnt=0;
            for(j=0;j<n;j++){
                  if(max<=a[i][j]){
                        max=a[i][j]>max?a[i][j]:max;
                        cnt++;
                  }else
                        break;
            }
            //printf("max=%d %d \n",max,cnt-1);
            for(k=0;k<n;k++){
                  min=a[k][cnt-1]<min?a[k][cnt-1]:min;
                  //printf("min=%d \n",min);
                  }
            if(min==max){
                  printf("%d %d %d\n",min,i,cnt-1);
                  flag=1;
                  }
            }
            //printf("%d %d",max,cnt);     //cnt表示最大值出现在的（列数-1）；
            //printf("\n");
      if(flag==0){
            printf("NONE\n");
      }
      return 0;
}
