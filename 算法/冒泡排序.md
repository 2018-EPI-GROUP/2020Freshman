```
void MP(int* p, int len)
{
	int i, j;
	int temp;
	for (j = 0; j < len - 1; j++)
	{
		for (i = 0; i < len - j - 1; i++)
		{
			if (*(p + i) > *(p + i + 1))
			{
				temp = *(p + i);
				*(p + i) = *(p + i + 1);
				*(p + i + 1) = temp;
			}
		}
		
	}
}
```
