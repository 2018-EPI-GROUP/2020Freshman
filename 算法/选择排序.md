```
void XZ(int* p, int len)
{
	int i;
	int j;
	int min;
	int temp;
	for (i = 0; i < len - 1; i++)
	{
		temp = *(p + i);
		min = i;
		for (j = i; j < len ; j++)
		{
		/*	if (p[j] < p[min])
			{
				min = j;
			}*/
			min = (p[j] < p[min]) ? j : min;
		}
		p[i] = p[min];
		p[min] = temp;

	}
}
```
