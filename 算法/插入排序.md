```
void CR(int* p, int len)
{
	int temp;
	int i;
	int j;
	for (i = 1; i < len; i++)
	{
		temp = *(p + i);
		j = i;
		while (j - 1 >= 0 && *(p + j - 1) >  temp )
		{
			*(p + j) = *(p + j - 1);
			j--;
		}
		*(p + j) = temp;

    }
}
```
